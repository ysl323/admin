# 部署到服务器 - 修复登录问题

## 📋 部署步骤

### 方式1: 上传脚本并执行（推荐）

1. **上传更新脚本到服务器**
   ```bash
   # 在本地执行，将脚本上传到服务器
   scp update-server-fix-login.sh root@47.97.185.117:/root/
   ```

2. **登录服务器**
   ```bash
   ssh root@47.97.185.117
   ```

3. **执行更新脚本**
   ```bash
   cd /root
   chmod +x update-server-fix-login.sh
   ./update-server-fix-login.sh
   ```

### 方式2: 直接在服务器执行命令

1. **登录服务器**
   ```bash
   ssh root@47.97.185.117
   ```

2. **进入后端目录**
   ```bash
   cd /www/wwwroot/english-learning/backend
   ```

3. **创建修复脚本**
   ```bash
   cat > fix-user-table.js << 'EOF'
   import { sequelize } from './src/config/database.js';
   import './src/models/index.js';
   import { User } from './src/models/index.js';

   async function fixUserTable() {
     try {
       await sequelize.authenticate();
       console.log('✅ 数据库连接成功');

       const tableInfo = await sequelize.getQueryInterface().describeTable('users');

       if (!tableInfo['is_super_admin']) {
         console.log('添加 is_super_admin 列...');
         await sequelize.getQueryInterface().addColumn('users', 'is_super_admin', {
           type: sequelize.Sequelize.BOOLEAN,
           defaultValue: false,
           allowNull: false
         });
         console.log('✅ 列添加成功');
       }

       await User.update(
         { isAdmin: true, isSuperAdmin: true },
         { where: { username: 'admin' } }
       );
       console.log('✅ admin用户已更新');

       await sequelize.close();
     } catch (error) {
       console.error('❌ 错误:', error.message);
       process.exit(1);
     }
   }

   fixUserTable();
   EOF
   ```

4. **创建密码重置脚本**
   ```bash
   cat > reset-admin-password.js << 'EOF'
   import { sequelize } from './src/config/database.js';
   import './src/models/index.js';
   import bcrypt from 'bcryptjs';
   import { User } from './src/models/index.js';

   async function resetAdminPassword() {
     try {
       await sequelize.authenticate();

       const newPassword = 'admin123';
       const hashedPassword = await bcrypt.hash(newPassword, 10);

       await User.update(
         { passwordHash: hashedPassword },
         { where: { username: 'admin' } }
       );
       console.log('✅ admin密码已重置');

       await sequelize.close();
     } catch (error) {
       console.error('❌ 错误:', error.message);
       process.exit(1);
     }
   }

   resetAdminPassword();
   EOF
   ```

5. **执行修复**
   ```bash
   node fix-user-table.js
   node reset-admin-password.js
   pm2 restart english-backend
   ```

## ✅ 验证修复

### 测试登录API
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 测试前端访问
```bash
curl -I http://47.97.185.117
```

## 🔑 登录凭据

```
用户名: admin
密码: admin123
```

## 📊 修复内容

1. ✅ 添加 `is_super_admin` 列到 users 表
2. ✅ 重置 admin 密码为 admin123
3. ✅ 设置 admin 为超级管理员
4. ✅ 重启后端服务
5. ✅ 测试登录功能

## 📝 注意事项

1. **备份建议**
   - 执行前建议备份数据库
   ```bash
   cp /www/wwwroot/english-learning/data/database.sqlite \
      /www/wwwroot/english-learning/data/database.sqlite.backup
   ```

2. **查看日志**
   ```bash
   # 后端日志
   pm2 logs english-backend

   # 错误日志
   tail -f /www/wwwroot/english-learning/logs/backend-error.log
   ```

3. **检查服务状态**
   ```bash
   pm2 status
   pm2 monit
   ```

## 🚀 完成后

1. 访问 http://47.97.185.117
2. 使用 admin / admin123 登录
3. 验证学习模式和课程功能

## ❓ 如果出现问题

1. 检查数据库权限
   ```bash
   ls -la /www/wwwroot/english-learning/data/
   ```

2. 检查PM2进程
   ```bash
   pm2 list
   pm2 logs
   ```

3. 检查Nginx配置
   ```bash
   nginx -t
   cat /etc/nginx/conf.d/english-learning.conf
   ```

4. 查看详细错误
   ```bash
   tail -100 /www/wwwroot/english-learning/logs/backend-error.log
   ```

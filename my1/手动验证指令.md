# 🔍 手动验证服务器文件完整性

## 当前进度报告

✅ **已完成**:
- 本地代码打包 (`deploy-20260310_210528.zip`)
- 文件上传到服务器
- 后端依赖安装 (405个包)
- PM2服务启动
- 环境配置更新
- Nginx配置完成

❌ **当前问题**:
- 网站访问返回500错误
- SSH连接经常超时/卡住

## 🎯 立即执行的验证命令

### 方式一：使用验证脚本
```powershell
.\服务器验证脚本.ps1
```

### 方式二：手动SSH验证
如果脚本卡住，请手动执行以下命令：

```bash
# 1. 连接服务器
ssh root@47.97.185.117
# 密码: Admin88868

# 2. 检查应用目录
ls -la /root/english-learning/

# 3. 检查后端文件
ls -la /root/english-learning/backend/src/server.js
ls -la /root/english-learning/backend/package.json

# 4. 检查前端文件  
ls -la /root/english-learning/frontend/index.html

# 5. 检查PM2服务状态
pm2 list
pm2 logs english-learning-backend --lines 10

# 6. 检查端口监听
netstat -tlnp | grep 3000

# 7. 检查Nginx
nginx -t
systemctl status nginx

# 8. 测试后端API
curl -I http://localhost:3000/api/health

# 9. 检查系统资源
free -h
df -h
```

## 🚨 如果SSH连接卡住

### 解决方案1：使用新终端
打开新的PowerShell/CMD窗口，重新连接

### 解决方案2：检查网络
```cmd
ping 47.97.185.117
telnet 47.97.185.117 22
```

### 解决方案3：使用SSH客户端
- 使用PuTTY或其他SSH客户端
- 设置连接超时时间

## 🔧 快速修复选项

如果验证发现问题，立即执行：

### 选项1：重启服务
```powershell
.\快速重启服务.ps1
```

### 选项2：修复500错误
```cmd
一键修复500.bat
```

### 选项3：紧急修复
```powershell
.\紧急修复500错误.ps1
```

## 📊 预期验证结果

### ✅ 正常状态应该显示：
- `/root/english-learning/` 目录存在
- `backend/src/server.js` 文件存在
- `frontend/index.html` 文件存在
- PM2显示 `english-learning-backend` 运行中
- 端口3000被监听
- Nginx配置测试通过

### ❌ 异常状态可能显示：
- 目录不存在或为空
- 关键文件缺失
- PM2服务停止或错误
- 端口未监听
- Nginx配置错误

## 🎯 下一步行动

根据验证结果：

1. **如果文件完整** → 重启服务解决500错误
2. **如果文件缺失** → 重新部署
3. **如果服务异常** → 检查日志并修复
4. **如果配置错误** → 更新配置文件

---

**💡 提示**: 如果SSH持续卡住，可以尝试重启服务器或联系服务提供商检查网络连接。
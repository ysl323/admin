# 多单词输入修复 - 测试指南

## 测试地址
http://47.97.185.117

## 登录信息
- 用户名: admin
- 密码: admin123

## 测试场景

### 场景1: 动态下划线测试

**目标**: 验证下划线长度随输入动态变化

**步骤**:
1. 登录系统
2. 进入任意课程 (例如: 新概念英语 > 第1课)
3. 找到多单词题目 (例如: "hello world")
4. 观察第一个输入框的占位符 (应该显示多个下划线)
5. 开始输入字符
6. **预期结果**: 每输入一个字符,下划线数量减少一个

**示例**:
- 输入前: `_____` (5个下划线,假设单词是 "hello")
- 输入 "h": `____` (4个下划线)
- 输入 "he": `___` (3个下划线)
- 输入 "hel": `__` (2个下划线)
- 输入 "hell": `_` (1个下划线)
- 输入 "hello": `` (无下划线)

### 场景2: 只清空错误单词测试

**目标**: 验证错误时只清空错误的单词,保留正确的单词

**步骤**:
1. 找到多单词题目 (例如: "hello world")
2. 在第一个输入框输入正确的单词 "hello"
3. 按空格键切换到第二个输入框
4. 在第二个输入框故意输入错误的单词 "test" (正确应该是 "world")
5. 按空格键提交答案
6. **预期结果**:
   - 显示错误提示: "有 1 个单词错误,请重新输入错误的单词"
   - 播放音频
   - 1.5秒后:
     - 第一个输入框 "hello" **保持不变** ✅
     - 第二个输入框 "test" **被清空** ✅
     - 焦点自动移到第二个输入框 ✅
     - 第二个输入框有红色下划线动画效果 ✅

### 场景3: 多个错误单词测试

**目标**: 验证多个错误时的处理

**步骤**:
1. 找到三个单词的题目 (例如: "good morning sir")
2. 输入: "good" (正确) + "evening" (错误) + "madam" (错误)
3. 按空格提交
4. **预期结果**:
   - 显示: "有 2 个单词错误,请重新输入错误的单词"
   - 1.5秒后:
     - "good" 保持不变 ✅
     - "evening" 被清空 ✅
     - "madam" 被清空 ✅
     - 焦点移到第一个错误的输入框 (第二个) ✅

### 场景4: 全部正确测试

**目标**: 验证全部正确时的行为

**步骤**:
1. 找到多单词题目
2. 输入所有正确的单词
3. 按空格提交
4. **预期结果**:
   - 显示 "正确!" ✅
   - 1秒后自动跳转到下一题 ✅
   - 自动播放下一题音频2遍 ✅

### 场景5: 键盘敲击声音测试

**目标**: 验证输入时有键盘敲击声音

**步骤**:
1. 确保浏览器音量开启
2. 在输入框中输入字符
3. **预期结果**: 每次按键都有短促的键盘敲击声音 (800Hz, 50ms)

## 已知行为

### 正常行为
1. 下划线长度动态变化 ✅
2. 输入框宽度固定 (基于目标单词长度) ✅
3. 空格键切换到下一个输入框 ✅
4. 最后一个单词按空格提交答案 ✅
5. 错误的单词有红色下划线动画 ✅
6. 只清空错误的单词 ✅
7. 自动聚焦到第一个错误的输入框 ✅

### 设计决策
1. **输入框宽度不动态变化**: 保持固定宽度让用户知道目标单词长度
2. **下划线动态变化**: 提供实时反馈,显示还需要输入多少字符
3. **保留正确单词**: 减少用户重复输入,提高学习效率

## 测试检查清单

- [ ] 动态下划线功能正常
- [ ] 只清空错误单词功能正常
- [ ] 键盘敲击声音正常
- [ ] 空格切换输入框正常
- [ ] 错误提示信息正确
- [ ] 自动聚焦功能正常
- [ ] 红色下划线动画正常
- [ ] 正确答案自动跳转正常

## 如果发现问题

请记录以下信息:
1. 具体操作步骤
2. 预期结果
3. 实际结果
4. 浏览器类型和版本
5. 截图或录屏

## 技术实现

### 动态下划线
```javascript
const getPlaceholder = (index) => {
  const currentLength = wordInputs.value[index]?.length || 0;
  const targetLength = wordParts.value[index]?.length || 0;
  const remainingLength = Math.max(targetLength - currentLength, 0);
  return '_'.repeat(remainingLength);
};
```

### 错误处理
```javascript
// 逐个检查每个单词
const hasErrors = [];
for (let i = 0; i < wordParts.value.length; i++) {
  const input = wordInputs.value[i].trim().toLowerCase();
  const expected = wordParts.value[i].toLowerCase();
  if (input !== expected) {
    hasErrors.push(i);
    wordErrors.value[i] = true;
  }
}

// 只清空错误的单词
hasErrors.forEach(index => {
  wordInputs.value[index] = '';
  wordErrors.value[index] = false;
});
```

## 部署信息
- 部署时间: 2025-03-09 15:13
- 版本: v1.2 (多单词输入修复版)
- 修改文件: LearningPage.vue
